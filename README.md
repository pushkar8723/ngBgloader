Angular Background Loader
=========================

Angular module to load background images asynchronously while showing a placeholder image. 
When background image loads, it fades in and replaces the placeholder image, 
giving page more readability while loading.